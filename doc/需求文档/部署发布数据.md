请执行以下界面优化和功能增强任务：  1. 表格标题修改：将当前显示为"版本列表"的表格标题更新为"部署发布数据"。  2. 表格列头配置：调整表格列头为以下精确顺序和名称：    - 租户    - 应用名    - 应用 ID    - 创建时间    - 总部署次数    - 手动部署次数    - 最新部署时间    - 回滚次数    - 修改部署配置次数    - 当前部署包版本数    - 统计周期    - 部署包合计大小（GB）    - 最大部署包（MB）    - 部署包平均大小（MB）    - 用途    - 接入状态  3. 表格功能增强：为表格添加横向滚动功能，确保在列数较多或屏幕宽度有限时，用户可以通过水平滚动查看所有列数据，同时保持表格头部在滚动时固定可见。  4. 查询功能扩展：在查询区域添加以下三个查询条件字段，每个字段应提供适当的输入控件（如下拉选择框或文本输入框）：    - 应用名：用于按应用名称筛选数据    - 用途：用于按应用用途类别筛选数据    - 接入状态：用于按接入状态筛选数据  确保所有修改后的界面元素布局合理、交互友好，且在不同屏幕尺寸下均能正常显示和操作。
